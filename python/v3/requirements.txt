# Core dependencies for Solar Heating System v3
# This file consolidates all requirements from the previous separate files

# Hardware interfaces (system-wide installation required)
# megabas>=1.0.0
# librtd>=1.0.0
# lib4relind>=1.0.0

# MQTT communication
paho-mqtt>=1.6.1
asyncio-mqtt>=0.16.0

# Data processing and analysis
numpy>=1.21.0
pandas>=1.3.0

# Web framework and API
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
aiohttp>=3.8.0  # Lightweight HTTP server for health checks

# Database
influxdb-client>=1.36.0

# TaskMaster AI integration
httpx>=0.25.0

# Configuration and utilities
python-dotenv>=1.0.0

# Logging and monitoring
structlog>=23.0.0

# Note: Hardware libraries (megabas, librtd, lib4relind) are installed system-wide
# and should be installed separately using the Sequent Microsystems installation scripts
